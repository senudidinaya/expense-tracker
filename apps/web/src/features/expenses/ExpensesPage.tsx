import { PagePlaceholder } from "../../app/PagePlaceholder";

/** Replaced by the real expenses screen in Task 20. */
export function ExpensesPage() {
  return <PagePlaceholder title="Expenses" task="Task 20" />;
}
