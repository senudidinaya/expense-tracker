import { PagePlaceholder } from "../../app/PagePlaceholder";

/** Replaced by the real settings screen in Task 23. */
export function SettingsPage() {
  return <PagePlaceholder title="Settings" task="Task 23" />;
}
